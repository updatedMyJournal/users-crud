{
  "name": "users-crud",
  "version": "1.0.0",
  "private": "true",
  "type": "module",
  "scripts": {
    "build-app": "npm run build-client && npm install && npm run start-server",
    "build-client": "cd client && npm run build-app && cd ..",
    "start-server": "node src/index.js",
    "dev": "nodemon src/index.js"
  },
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "date-fns": "^2.29.3",
    "express": "^4.18.2",
    "mongoose": "^6.8.1",
    "mongoose-paginate-v2": "^1.7.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
